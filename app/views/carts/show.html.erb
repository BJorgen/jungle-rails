<section class="cart-show">

  <header class="page-header">
    <h1>My Cart</h1>
  </header>

  <% if !cart.empty? %>
    <%=render 'full_cart', enhanced_cart: enhanced_cart%>
  <% else %>  
    <%=render 'empty_cart'%>
  <% end %>
  

</section>
